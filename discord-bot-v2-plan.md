# 📄 [계획서] 젤리봇 2차 기능 확장 (Music & TTS 고도화 + 신규 모듈 제안)

> 이 문서는 기존 `discord-bot.md`(음악 & TTS 봇 최초 기획서)에 이어지는 후속 계획서입니다.
> 현재 구현된 기능(재생/정지/대기열/퇴장/TTS채널등록·해제)을 기준으로, 다음 단계에서 추가하면
> 좋을 기능들을 정리했습니다.

---

## 1. 현재 상태 요약

| 모듈 | 구현된 커맨드 |
| :--- | :--- |
| 음악 재생 | `/재생`, `/정지`, `/재생목록`, `/퇴장` |
| TTS | `/채널등록`, `/채널해제` |

* 대기열/재생 상태는 프로세스 메모리(`Map<guildId, ServerQueue>`)에서 서버별로 격리 관리
* TTS 채널 등록 정보만 Redis에 영속화 (재시작 후에도 유지)
* Render 등 PaaS 배포를 전제로 한 헬스체크 서버(Express/http) 포함

---

## 2. Phase 1 — 기존 음악 모듈 고도화 (우선순위: 높음)

지금 구조(`playerOrchestrator`, `queueStore`, `connectionManager`)를 그대로 확장하는 선에서
끝낼 수 있는, 사용자 체감이 큰 기능들입니다.

### F-06. 일시정지 / 재개 (`/일시정지`, `/재개`)
* **기능 요약**: 현재 곡을 대기열 초기화 없이 멈추고, 다시 이어서 재생
* **상세 명세**:
    * `AudioPlayer.pause()` / `resume()` 호출 (기존 `/정지`는 대기열까지 비우므로 별개 기능으로 유지)
    * 이미 일시정지 상태에서 `/일시정지` 재호출 시 안내 메시지만 반환 (에러 아님)

### F-07. 스킵 (`/스킵`)
* **기능 요약**: 현재 곡을 건너뛰고 대기열의 다음 곡으로 즉시 전환
* **상세 명세**:
    * 현재 `AudioPlayerStatus.Idle` 이벤트로 처리하는 "자동 다음 곡" 로직을 그대로 재사용
    * 대기열이 비어있으면 `/정지`와 동일하게 처리 (안내 메시지 + 대기 후 자동 퇴장 타이머 가동)

### F-08. 반복 재생 (`/반복 <끄기 | 한곡 | 전체>`)
* **기능 요약**: 반복 모드 설정
* **상세 명세**:
    * `ServerQueue`에 `loopMode: 'off' | 'track' | 'queue'` 필드 추가
    * `한곡`: 현재 곡 종료 시 큐에 다시 push하지 않고 그대로 재생
    * `전체`: 곡 종료 시 큐 맨 끝으로 이동 (재생목록을 순환)

### F-09. 셔플 (`/셔플`)
* **기능 요약**: 현재 대기열 순서를 무작위로 섞기
* **상세 명세**: Fisher–Yates 셔플로 `ServerQueue.tracks` 배열만 섞고, 현재 재생 중인 곡은 그대로 유지

### F-10. 볼륨 조절 (`/볼륨 <0-100>`)
* **기능 요약**: 서버별 재생 볼륨 조절
* **상세 명세**: `AudioResource`의 `volume` 속성(`inlineVolume: true` 필요) 사용, `ServerQueue`에 마지막 설정값 저장해서 다음 곡에도 유지

### F-11. Now Playing 임베드 버튼 컨트롤
* **기능 요약**: `/재생` 시 나오는 Embed에 ⏸ ⏭ 🔁 ⏹ 버튼을 붙여서 명령어 없이 제어
* **상세 명세**:
    * `ActionRowBuilder` + `ButtonBuilder`로 구성, `interactionCreate`에서 버튼 클릭도 함께 처리
    * 버튼 클릭자가 음성 채널에 없는 경우 방지 로직 필요 (권한 체크)

### F-12. 검색 결과 선택 UI
* **기능 요약**: 키워드 검색 시 최상위 결과 1개를 바로 재생하는 대신, 상위 5개를 보여주고 고르게 함
* **상세 명세**: `StringSelectMenuBuilder`로 제목/채널명/길이를 보여주는 선택지 제공, URL 직접 입력 시엔 기존처럼 즉시 재생(스킵)

---

## 3. Phase 2 — TTS 모듈 고도화

### F-13. TTS 언어/속도 개인 설정 (`/tts설정 <언어>`)
* **기능 요약**: 사용자별 TTS 언어(예: ko, en, ja) 선택
* **상세 명세**: Redis에 `user:{userId}:ttsLang` 형태로 저장, 미설정 시 기존처럼 한국어 기본값

---

## 4. Phase 3 — 신규 모듈 (⚠️ 방향 결정 필요)

아래 두 방향 중 하나(또는 둘 다 순차적으로)를 제안합니다. GitHub 저장소 설명에는
"명단 관리 및 포스트 자동 정렬"이 언급되어 있는데, 현재 코드에는 없고 과거
`refactor/gy/remove-forum-crud` 브랜치 이력으로 미루어 한 번 만들었다가 제외하신 것으로
보입니다. 다시 살릴지, 완전히 새 방향으로 갈지 결정이 필요합니다.

### 옵션 A. 범용 서버 유틸리티 모듈
음악/TTS와 무관하게, 일반적인 커뮤니티 서버에서 자주 쓰이는 기능들입니다.

* **F-14. 환영 메시지**: 신규 멤버 입장 시 지정 채널에 인사 메시지 자동 전송
* **F-15. 리액션 역할**: 특정 메시지에 이모지 반응 시 역할 자동 부여/해제
* **F-16. 서버 통계**: `/통계`로 오늘 음악 재생 횟수, 가장 많이 들은 곡 Top 5 등 (Redis 카운터 활용)

### 옵션 B. 명단 관리 + 포스트 자동 정렬 모듈 (저장소 기존 설명 기반)
스터디/동아리형 서버를 염두에 둔 방향입니다. 실제 타겟이 이쪽이라면 구체적인 요구사항
(출석 체크 방식, 포럼 채널 정렬 기준 등)을 여쭤보고 별도 계획서로 상세화가 필요합니다.

* **F-17. 멤버 명단 등록/조회** (`/명단등록`, `/명단조회`)
* **F-18. 포럼 포스트 자동 정렬** — 특정 포럼 채널의 글을 태그/작성일 기준으로 자동 정렬

---

## 5. 우선순위 제안

| 순서 | 항목 | 이유 |
| :--- | :--- | :--- |
| 1 | Phase 1 (F-06~F-12) | 기존 음악 기능의 명백한 공백을 메움, 리스크 낮음 |
| 2 | Phase 2 (F-13) | 작은 규모, Phase 1과 함께 진행 가능 |
| 3 | Phase 3 | 방향(옵션 A/B) 결정 후 별도 계획서로 상세화 |

---

## 6. 비기능 요구사항 (기존 기조 유지)

* 슬래시 커맨드 응답 3초 이내 `deferReply`
* 서버(Guild)별 상태 격리 원칙 유지 (`Map<guildId, ServerQueue>` 확장 시에도 동일)
* 버튼/셀렉트 메뉴 등 신규 컴포넌트도 기존 `interactionCreate` 핸들러 구조 안에서 라우팅
