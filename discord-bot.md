# 📄 [기획서] 디스코드 음악 & TTS 봇 (젤리봇)

---

## 1. 프로젝트 개요 (Project Overview)

* **프로젝트명**: 디스코드 음악 & TTS 봇 (젤리봇)
* **개발 환경 & 기술 스택**:
    * **Runtime**: Node.js (v18+ 이상 권장)
    * **Language**: TypeScript
    * **Library**: `discord.js` (v14+), `@discordjs/voice`
    * **Core Modules**: `@distube/ytdl-core` 또는 `play-dl` (유튜브 스트리밍), `google-tts-api` (TTS 변환), `prism-media` / `ffmpeg-static` (오디오 코덱)
* **개발 목적**:
    * 디스코드 음성 채널에서 고품질 유튜브 음악 연속 재생(Queue 관리) 및 지정된 텍스트 채널 감지를 통한 음성 변환(TTS) 인터럽트 기능을 제공하는 봇 개발
* **타겟 사용자**:
    * **일반 사용자 (User)**: 음성 채널에서 음악을 감상하거나 특정 텍스트 채널에 글을 작성해 음성으로 메시지를 전달하고자 하는 디스코드 사용자
    * **서버 관리자 (Server Admin)**: 음성 채널 음악 및 TTS 전용 채널을 관리하는 서버 운영진

---

## 2. 사용자 여정 (User Journey)

### 2.1 일반 사용자 (User Flow)
* **1단계 (음성 채널 입장)**: 디스코드 음성 채널 입장 ➔ `/재생` 명령어로 음악 검색 및 봇 호출
* **2단계 (음악 재생 및 연속 재생)**: 유튜브 링크 또는 검색어 입력 ➔ 대기열(Queue) 자동 등록 ➔ 현재 곡 종료 시 다음 곡 자동 재생
* **3단계 (TTS 채널 활용)**: 서버 관리자가 지정한 TTS 전용 채널에 텍스트 작성 ➔ 봇이 이를 감지하여 음성 채널로 TTS 출력 (음악 일시정지 후 재생 ➔ 종료 시 음악 재개)
* **4단계 (음악 정지 및 종료)**: 음악 재생 중단 시 `/정지` 명령어로 대기열 초기화 및 음악 중단

### 2.2 서버 관리자 (Server Admin Flow)
* **1단계 (봇 초대 및 권한 설정)**: 디스코드 OAuth2 링크로 봇 서버 초대 ➔ 음성/텍스트 채널 접근 권한 부여
* **2단계 (TTS 전용 채널 설정)**: TTS로 읽어줄 텍스트 채널에서 `/채널등록` 실행 ➔ 해당 채널의 메시지가 자동 TTS로 변환되도록 세팅
* **3단계 (TTS 채널 해제)**: 더 이상 TTS를 사용하지 않을 경우 해당 채널에서 `/채널해제` 실행

---

## 3. 화면 및 기능별 세부 명세 (Specifications)

### A. 음악 재생 & 대기열 모듈 (Music Player Module)

#### F-01. 음악 재생 및 검색 (`/재생 <검색어|URL>`)
* **기능 요약**: 유튜브 링크 또는 키워드 기반 음성 스트리밍 재생
* **상세 명세**:
    * 사용자가 위치한 음성 채널에 봇이 자동 입장
    * 키워드 입력 시 유튜브 최상위 검색 결과 자동 스트리밍
    * 이미 재생 중인 음악이 있을 경우 자동으로 **서버별 대기열(Queue)**에 등록 및 Embed 메시지 출력

#### F-02. 음악 정지 (`/정지`)
* **기능 요약**: 현재 재생 중인 음악 중단 및 대기열 초기화
* **상세 명세**:
    * 현재 재생 중인 오디오 플레이어 정지
    * 남아있는 대기열(Queue) 목록을 모두 비우고 봇의 음성 재생 상태 초기화

#### F-03. 자동 연속 재생 및 대기열 관리 (Auto Next Play & Queue)
* **기능 요약**: 대기열 순서에 따른 끊김 없는 자동 재생
* **상세 명세**:
    * `@discordjs/voice`의 `AudioPlayerStatus.Idle` 이벤트를 감지하여 현재 곡 종료 시 대기열의 다음 곡(`.shift()`) 자동 재생
    * 대기열이 비어있을 경우 3분간 대기 후 리소스 절약을 위해 음성 채널 자동 퇴장
    * `/재생목록`: 현재 재생 중인 곡, 남은 곡 목록 표출

---

### B. TTS 및 인터럽트 제어 모듈 (TTS & Priority Control)

#### F-04. TTS 채널 등록 및 해제 (`/채널등록`, `/채널해제`)
* **기능 요약**: 특정 텍스트 채널을 TTS 자동 읽기 채널로 지정 및 해제
* **상세 명세**:
    * `/채널등록`: 해당 커맨드가 입력된 텍스트 채널 ID를 서버 설정(Memory/DB)에 저장. 이후 이 채널에 올라오는 일반 텍스트 메시지를 봇이 자동으로 감지하여 TTS 음성 변환 수행
    * `/채널해제`: 등록된 TTS 채널 설정을 제거하여 일반 텍스트 채널로 원복

#### F-05. 음악-TTS 우선순위 제어 (Priority Logic)
* **기능 요약**: 음악 재생 중 TTS 감지 시 우선순위에 따른 인터럽트 처리
* **상세 명세**:
    * **인터럽트 (Interrupt) 방식 적용**:
        1. 등록된 TTS 채널에 메시지 입력 발생 (`messageCreate` 이벤트 감지)
        2. 음악 재생 중일 경우 현재 음악 `AudioPlayer` 일시 정지 (`pause()`)
        3. `google-tts-api`로 변환된 음성 스트림 즉시 출력
        4. TTS 재생 완료 (`Idle` 상태 감지) 후 일시 정지된 음악 자동 재개 (`resume()`)

---

## 4. 실시간 통신 및 예외 처리 UX 기획

### 4.1 음성 채널 이벤트 및 재연결 정책
1. **음성 채널 연결**: `/재생` 시 사용자가 위치한 Voice Channel ID를 파악하여 `joinVoiceChannel()` 실행
2. **Heartbeat & Voice State 감지**: 디스코드 음성 게이트웨이와의 PING 수신을 통해 세션 유효성 확인
3. **네트워크 유실 및 재연결**:
    * Voice Connection 끊김 발생 시 Exponential Backoff 알고리즘 기반 자동 재연결 시도
    * 재연결 실패 시 대기열 상태 보존 후 텍스트 채널에 알림 메시지 출력

### 4.2 서버(Guild)별 상태 격리
* **`Map<guildId, ServerQueue>` 데이터 구조**: 각 디스코드 서버별로 독립적인 대기열(Queue), 현재 재생 곡, TTS 지정 채널 ID, AudioPlayer, VoiceConnection 객체를 분리하여 상호 간섭 차단

---


## 5. 주요 API 및 슬래시 커맨드 명세

### A. 음악 재생 & 대기열 명령어 (Music Commands)
| 구분 | Command     | Parameter | 설명 |
| :--- |:------------| :--- | :--- |
| **음악 재생** | `/재생`     | `검색어` (필수: URL 또는 키워드) | 유튜브 음악 재생, 음성 채널 자동 입장 및 대기열 등록 |
| **음악 정지** | `/정지`     | - | 음악 재생 중단 및 대기열 목록 전체 초기화 |
| **대기열 조회** | `/재생목록` | - | 현재 대기열 목록 및 재생 중인 곡 정보 출력 |
| **퇴장** | `/퇴장`     | - | 음성 채널 퇴장 및 상태 초기화 |

### B. TTS 채널 관리 명령어 (TTS Commands)
| 구분 | Command | Parameter | 설명 |
| :--- | :--- | :--- | :--- |
| **TTS 채널 등록** | `/채널등록` | - | 커맨드를 입력한 텍스트 채널을 자동 TTS 읽기 채널로 지정 |
| **TTS 채널 해제** | `/채널해제` | - | 등록된 TTS 읽기 채널 설정 해제 |

---

## 6. 비기능적 요구사항 및 성능 목표

### 6.1 성능 및 응답 지표
* **슬래시 커맨드 응답 속도**: 디스코드 API 제약에 맞춰 3초 이내 초기 응답(`deferReply`) 수행
* **오디오 스트리밍 딜레이**: 재생 요청 후 음성 출력까지 **1.5초 이내** 진입

### 6.2 메모리 및 리소스 관리
* **FFmpeg 리소스 해제**: 스트림 종료 시 `prism-media` 및 FFmpeg 프로세스 즉시 `destroy()` 호출하여 메모리 누수 방지
* **자동 퇴장 세션 정리**: 음성 채널에 활성 사용자가 0명일 경우 1분 후 대기열 메모리 해제 및 음성 채널 퇴장